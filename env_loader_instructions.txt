"""
Optional: Load environment variables from .env file for local development
This is helpful so you don't have to set env vars manually each time

Usage:
1. Create a .env file with your credentials (see .env.example)
2. Uncomment the lines below in app.py (at the top, before app = Flask(__name__))

# Uncomment these lines in app.py to use .env file:
# from dotenv import load_dotenv
# load_dotenv()
"""

# To use this, install python-dotenv:
# pip install python-dotenv

# Then add to requirements.txt:
# python-dotenv==1.0.0
